# --- audio ---
sounddevice==0.5.1
numpy>=1.26,<3
soxr>=0.5.0

# --- speech to text (local) ---
faster-whisper>=1.1.0

# --- text to speech (local) ---
piper-tts>=1.3.0

# --- traduccion (nube, con fallback local) ---
# Se instalan las dos; se usa la que diga config.yaml -> traduccion.proveedor
anthropic>=0.40.0
google-genai>=1.0.0

# --- varios ---
PyYAML>=6.0
# Bajar voces con un CA bundle propio, no el del SO (traductor/tts.py)
certifi>=2024.0.0
# Guarda config.yaml sin perder los comentarios que lo explican
ruamel.yaml>=0.18
rich>=13.0
# Ventana nativa (ventana.py): quien esta de turno no deberia necesitar una
# terminal. En Windows usa EdgeChromium (WebView2, ya viene con Windows 10/11).
pywebview>=5.0

# --- solo para probar con cultos ya streameados (tools/bajar_culto.py) ---
yt-dlp>=2025.1.1
